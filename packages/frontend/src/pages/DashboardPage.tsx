// Phase 2 implementation — placeholder
export default function DashboardPage() {
  return (
    <div>
      <h1 style={{ margin: '0 0 8px', color: '#1F3864' }}>Dashboard</h1>
      <p style={{ color: '#718096' }}>Select a release to view metrics. Import data first if no releases appear.</p>
      {/* ReleaseSelector, MetricCards, Charts wired in Phase 2 */}
    </div>
  );
}
